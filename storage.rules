rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Cho phép tất cả authenticated users đọc và ghi tất cả
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
